"""Parallel RL league trainer for fixed-opponent experiments.

This package intentionally mirrors the real training flow in python_agent_v8,
while keeping the current implementation unchanged. The key difference is that
it allows a fixed opponent mode: self, heuristic, minimax, or mixed.
"""
